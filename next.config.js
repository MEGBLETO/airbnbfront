/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.google.com', 'source.unsplash.com', 'randomuser.me', 'i.imgur.com', 'example.com', 'media.gettyimages.com'],
},
}

module.exports = nextConfig
